# borrador

Este repositorio contiene un sencillo ejemplo en Python para calcular el Valor Actual Neto (VAN) de una serie de flujos de efectivo futuros.

Para ejecutar el ejemplo incluido:

```
python npv.py
```

El módulo `npv.py` también puede importarse para utilizar la función `npv` en otros proyectos.
